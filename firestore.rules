rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 一名帳戶持有人只能存取以自己的 Firebase Auth UID 命名的資料文件。
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
